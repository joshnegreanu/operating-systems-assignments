Assignment 4: Multi-threaded Producer Consumer Counter
Operating Systems June 5, 2023
Josh Negreanu

To create the myCounter executable, run the following command in the directory:

    gcc myCounter.c -pthread -o myCounter

To run the myCounter executable, simply type:

    ./myCounter

To remove the myCounter executable, run the following command:

    rm -f myCounter